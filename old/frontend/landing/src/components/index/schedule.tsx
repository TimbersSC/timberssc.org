import React from 'react'

import { Box } from '@mantine/core'

export const ScheduleComponent = (): JSX.Element => {
  return (
    <Box
      id="schedule"
      sx={{
        maxWidth: '1335px',
        padding: '5em 0',
      }}
    >
      schedule
    </Box>
  )
}
